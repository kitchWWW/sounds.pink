# To Build:

* run `$ pyinstaller --windowed soundsPink.py` **Iportant: don't include --onefile, as the file structure will be un-editable**
* run `$ python TCLChanger/TCLChanger.py`
* You should now be able to open the app